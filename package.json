{
  "name": "draw-steel-sheet-root",
  "private": true,
  "description": "Monorepo for the Draw Steel TTRPG Character Sheet",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @iron-scribe/web-ui",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
